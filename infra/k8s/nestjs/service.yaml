apiVersion: v1
kind: Service
metadata:
  name: nestjs-service
  namespace: shoptik
  labels:
    app.kubernetes.io/name: nestjs-service
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: shoptik
spec:
  type: ClusterIP
  ports:
    - port: 5001
      targetPort: 5001
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: nestjs-service
