# Prevent pnpm from hoisting all deps to root node_modules
# Each workspace package keeps its own node_modules
shamefully-hoist=false
auto-install-peers=true
dedupe-peer-dependents=true

# Keep the lockfile clean
prefer-workspace-packages=true
